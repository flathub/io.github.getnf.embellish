id: io.github.getnf.embellish
runtime: org.gnome.Platform
runtime-version: "46"
sdk: org.gnome.Sdk
command: embellish

sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --share=network
  - --filesystem=~/.local/share/fonts:create
  - --filesystem=xdg-download:rw
  - --device=dri
modules:
  - name: embellish
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/golang/bin
      env:
        GOBIN: /app/bin
        GOROOT: /usr/lib/sdk/golang
    build-commands:
      - go install -v -tags=terminal,flatpak -ldflags "-s -w" -mod=vendor .
      - mv $GOBIN/embellish $GOBIN/embellish-tui
      - go install -v -tags=gui,flatpak -ldflags "-s -w" -mod=vendor .
      - install -Dm644 $FLATPAK_ID.desktop /app/share/applications/$FLATPAK_ID.desktop
      - install -Dm644 $FLATPAK_ID.svg /app/share/icons/hicolor/scalable/apps/$FLATPAK_ID.svg
      - install -Dm644 $FLATPAK_ID.metainfo.xml /app/share/metainfo/$FLATPAK_ID.metainfo.xml
    sources:
      - type: archive
        url: https://github.com/getnf/embellish/archive/refs/tags/v0.3.0.tar.gz
        sha256: 3696106a070ff052cefec08d71f11bccd1b24f27eab96f7a209b23b4247f34e8

      - type: file
        url: https://raw.githubusercontent.com/getnf/embellish/020373c5c83b9cda9d7121a76fb5cfcfafded2f2/io.github.getnf.embellish.metainfo.xml
        sha256: 3bbe244d675d6cb1abc210ef91bf1dfe8c7b4e301fe72be8a4eeae44006fb0c3

      - go.mod.yml
